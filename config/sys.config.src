[
  {hpr, [
    {aws_config, [
      {access_key_id, {$AWS_ACCESS_KEY_ID}},
      {secret_access_key, {${AWS_SECRET_ACCESS_KEY}}},
      {aws_region, {$AWS_DEFAULT_REGION}},
      {s3_scheme, "https://"},
      {s3_host, {$AWS_S3_HOST}}
  ]}
].
